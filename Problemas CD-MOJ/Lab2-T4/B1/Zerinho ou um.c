#include <stdio.h>
#include <locale.h>

int main(){

setlocale (LC_ALL, "");

int A, B, C;

printf ("Zerinho ou... Um!");
scanf ("%d%d%d", &A, &B, &C);

if (A == 0 || B == 0 || C == 0 || A == 1 || B == 1 || C == 1){
    if (){


    }
}

return 0;
}
